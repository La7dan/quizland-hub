import { executeSql } from './apiService';

// This file is kept as a placeholder but the cleanDummyData functionality has been removed
// as requested by the user to prevent accidental data deletion

// Export an empty object to maintain imports
export const cleanDatabaseServicePlaceholder = {};
